# 단위·통합 테스트용 인메모리 DB (Maven Surefire에서 spring.profiles.active=test)
spring:
  datasource:
    url: jdbc:h2:mem:am_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

logging:
  level:
    com.am.backoffice: WARN
